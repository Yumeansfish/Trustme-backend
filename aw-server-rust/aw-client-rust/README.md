# aw-client-rust

Rust client utilities used by Trust-me's backend workspace.

This crate remains internal to the monorepo. If you need the currently supported
release surface, start from the top-level backend README instead of treating this
crate as a standalone product.
