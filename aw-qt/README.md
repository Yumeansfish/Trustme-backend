# aw-qt

Qt tray manager for Trust-me's Python service stack.

This module is kept inside the backend workspace for packaging compatibility.
For the currently supported build and release flow, start from the top-level
backend README and workflow files.
