Mirrored dashboard contract fixtures.

These files are intentionally kept in sync with the matching fixtures under
`frontend/test/fixtures/dashboard/`.

They represent stable DTO and settings examples that both repos should test
against during the frontend/backend split.
